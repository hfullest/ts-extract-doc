import { Symbol } from 'ts-morph';
import { Document } from '../interface';

/** 从React类组件中提取文档 */
export const collectDocFromClassComponent = (typeSymbol: Symbol): Document | null => {
  return null;
};
