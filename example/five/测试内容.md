### App


### removeAntdTwoCharSpace

移除antd两个字符空格
 主要针对antd两个字符间空格问题
注意：这是个hack方法


### IconFontParams

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|type |图标名称|string|-|-|
|style ~(可选)~ |-|React.CSSProperties|-|-|
|fill ~(可选)~ |-|string|-|-|
|size ~(可选)~ |图标尺寸|string | number|-|-|


### IconFont


### mockAxios1


### mockAxios2


### mockAxios3


### PickPaneExample


### PickPane

选择面板


### PickPaneAvatarProps


### IPane

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|value |传进来的列表数据|D[]|-|-|
|keyPropName ~(可选)~ |唯一字段属性名 默认值和`titlePropName`值相同|keyof D | null | ((value: D) => string)|-|-|
|titlePropName ~(可选)~ |标题属性名  默认为`title`|keyof D | null | ((value: D) => React.ReactNode)|-|-|
|descPropName ~(可选)~ |描述属性名 默认为 `desc`|keyof D | null | ((value: D) => React.ReactNode)|-|-|
|avatarPropName ~(可选)~ |头像属性名 默认 `avatar`|keyof D | null | ((value: D) => React.ReactNode)|-|-|
|header ~(可选)~ |内容标题区，支持函数式|React.ReactNode | ((values: D[]) => React.ReactNode)|-|-|
|clearBtnText ~(可选)~ |全部清除按钮文案|string|-|-|
|showAllClear ~(可选)~ |是否展示清除全部按钮|boolean|-|-|
|emptyText ~(可选)~ |无数据展示的页面|React.ReactNode | (() => React.ReactNode)|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|onChange ~(可选)~ |内部值变化后的回调|(values: D[]) => void|-|-|


### PickTemplateType


### PickTemplateProps


### PickRenderType


### PickPaneMustRequired

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|value |传进来的列表数据|IPane<D>["value"]|-|-|
|onChange ~(可选)~ |内部值变化后的回调|IPane<D>["onChange"]|-|-|


### PartialPickPaneMustRequired


### IPickTemplateRequired


### IPickRenderRequired

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|template ~(可选)~ |-|never|-|-|
|templateProps ~(可选)~ |-|never|-|-|
|pickRender |自定义渲染选择组件或者组件元素|PickRenderType<D>|-|-|


### PickTemplateRenderRequired


### IPick


### BasicPickPaneProps

PickPane 属性声明

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|paneConfig ~(可选)~ |结果面板配置|Omit<IPane<D>, keyof PickPaneMustRequired<D>>|-|-|
|className ~(可选)~ |-|string|-|-|
|style ~(可选)~ |-|React.CSSProperties|-|-|
|width ~(可选)~ |容器宽度|React.CSSProperties["width"]|-|-|
|height ~(可选)~ |容器高度|React.CSSProperties["height"]|-|-|


### PickPaneProps


### computePropName


### UiButtonExample


### AAAAAA

? 为什么需要使用antd的Button组件进行封装
    1. antd提供了很多样式类型，但NativeButton暂未完全实现，例如：link按钮等
    2. 对antd按钮定制化的样式，同样也可以通过类名的方式给其他antd组件内部按钮添加样式，例如：Modal.confirm的按钮等
    3. 如果后续的样式及功能迭代需要复用antd的Button组件，使用NativeButton难维护和扩展


### UiButtonProps

UiButton


### CompatButtonProps


### UiButton


### ArrowFunction

 AB测试函数


### DeclarationFunction

这个是DelcarationFunction 的描述

这里是对`DeclarationFunction`函数的补充描述，这里可以使用`markdown`语法

### ErrorBound

错误边界


### ExampleInterface ~(3.5.19)~

接口

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|a |a属性|string|'hahah'|-|
|button ~(可选)~ |`button`描述  button的==内容==区|UiButtonProps|-|3.2.15|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|bb |接口函数定义|(p1: string ,      p2?: number   ) => string|-|-|

这里是内容

其他的内容区内容

```tsx
 const a = 123;
```

### ExampleClass

这个是对ExampleClass类的描述

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|a |a属性|string|"124"|-|
|button ~(可选)~ |`button`描述  button的==内容==区|UiButtonProps|-|3.2.15|
|cccPrivate |-|-|"1234"|-|
|dddPublic |测试公共方法|number|12|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|bb |接口函数定义|(p1: string ,      p2?: number   ) => string|() => ""|-|

#### 静态属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|fffStaticProp |-|boolean|true|-|

#### 静态方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|eeeStaticMethod |这个是静态方法|-|(a: number, b?: string) => {     return 456;   }|-|


### PaneBox


### PickBox


### AllPickTemplates


### dealTemplate


### AntdButtonProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|autoInsertSpaceInButton ~(可选)~ |只有两个字符时是否插入空格 默认`false`   注意：该属性非`antd`提供的属性，而是自定义的属性，是对`antd`按钮功能的增强|boolean|-|-|
|loading ~(可选)~ |是否`loading`  对`antd`按钮的`loading`功能进行了扩展，支持`loading`自定义配置|| boolean   | {          loading?: boolean;     /** type类型，默认值为`cover`      *  - cover `loading`图标覆盖按钮，会覆盖按钮文本      *  - contain `loading`图标包含在按钮中，不会覆盖按钮文本      */     type?: "cover" | "contain";          icon?: React.ReactNode;   }|-|-|
|type ~(可选)~ |按钮类型，在`antd`的`type`基础上进行了扩展  扩展的类型： - `minority` 次按钮 - `warn` 警告按钮 - `danger` 危险按钮 该类型样式等同于`type=primary danger=true`配置 - `textlink` 链接文本类型，和`link`的区别在于`link`更像超链接，而`textlink`更像链接型按钮 - `ghostLight`  按钮组一般使用主按钮`primary`和次按钮`minority` （注意：之前使用的次按钮为`ghost`，为统一规范，`ghost`不再推荐为次按钮使用，请使用`minority`代替）|| ButtonProps["type"]   | "minority"   | "warn"   | "danger"   | "ghostLight"   | "textlink"|-|-|
|disabled ~(可选)~ |按钮失效状态 对`antd`的属性进行了扩展|| boolean   | {          tooltip?: TooltipProps;   }|-|-|
|tooltip ~(可选)~ |按钮tooltip提示，当按钮禁用时该属性会失效   如果需要按钮禁用时提示请使用`disabled.tooltip`|TooltipProps | null|-|-|
|align ~(可选)~ |按钮内容对齐方式 默认`center`|"start" | "center" | "end"|-|-|
|padding ~(可选)~ |按钮内联 padding 值，如果为false 则为0|false | string|-|-|


### AntdButton


### PickCascaderProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|placeholder ~(可选)~ |-|string|-|-|
|isLeafPropName ~(可选)~ |叶子节点标志属性名 默认`isLeaf`|keyof T | null | ((value: T) => boolean)|-|-|
|keyPropName ~(可选)~ |唯一字段属性名 默认 `key`|keyof T | null | ((value: T) => string)|-|-|
|childrenPropName ~(可选)~ |嵌套子元素属性名 默认 `children`|keyof T | null | ((value: T) => T[])|-|-|
|titlePropName ~(可选)~ |标题属性名  默认为`title`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|descPropName ~(可选)~ |描述属性名 默认为 `desc`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|avatarPropName ~(可选)~ |头像属性名 默认 `avatar`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|breadcrumbPropName ~(可选)~ |面包屑展示属性名 默认取与`titlePropName`的值相同|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|showSearch ~(可选)~ |是否展示搜索框，默认`true`|boolean|-|-|
|showCheckAll ~(可选)~ |是否展示全选框  默认`true`|boolean|-|-|
|scrollLoad ~(可选)~ |是否使用滚动加载，和`pagination`互斥，且优先级比`pagination`低， 如果未开启分页功能，则默认使用滚动懒加载，也可手动关闭懒加载|boolean|-|-|
|loadingConfig ~(可选)~ |-|Exclude<ListProps<T>["loading"], boolean>|-|-|
|pagination ~(可选)~ |-|ListProps<T>["pagination"] | boolean|-|-|
|maxHeight ~(可选)~ |列表最大高度，注意不是整个组件的最大高度|React.CSSProperties["maxHeight"]|-|-|
|minHeight ~(可选)~ |列表最小高度，注意不是整个组件的最小高度|React.CSSProperties["minHeight"]|-|-|
|height ~(可选)~ |列表高度，注意不是整个组件的高度|React.CSSProperties["height"]|-|-|
|preserveDataMode ~(可选)~ |保留数据的模式 默认`only-child`   - `only-child` 只保留子节点  - `priority-parent` 优先保留父节点(如果子节点全选则仅保留父节点)  - `all` 父节点和子节点都保留|"only-child" | "priority-parent" | "all"|-|-|
|preserveAuxiliaryProps ~(可选)~ |是否保留节点辅助属性，默认`false`,如果启用该属性，需要通过`symbol`才能获取属性|boolean|-|-|
|usingLazyLoad ~(可选)~ |是否使用懒加载，点击加载并进入下级节点，默认`false`|boolean|-|-|
|counter ~(可选)~ |计数区域定制|| boolean     | ((                  current: [checkedNodes: T[], nodes: T[]],                  global: [checkedNodes: T[], nodes: T[]]       ) => React.ReactNode)|-|-|
|virtualRootNode ~(可选)~ |虚拟根节点名称   当数据的顶级为多个子节点集合时，为了让顶层子节点展示全需要在此基础上设置虚拟根节点  虚拟根节点不参与计算，只进行展示  如果设置为`false`，则不启用虚拟根节点，可手动指定`ReactNode`，默认为`root`|false | React.ReactNode|-|-|
|breadcrumbProps ~(可选)~ |面包屑配置|BreadcrumbProps|-|-|
|emptyText ~(可选)~ |无数据展示的页面|React.ReactNode | (() => React.ReactNode)|-|-|
|rules ~(可选)~ |自定义规则 根据规则处理节点数据|{          checkable?: (       node: T,       allNodesMap: Map<string, T & DataNodeType>     ) => boolean;     disabledCheckbox?: (       node: T,       allNodesMap: Map<string, T & DataNodeType>     ) => boolean;          disabled?: (node: T, allNodesMap: Map<string, T & DataNodeType>) => boolean;   }|-|-|


### PickCascader


### DataNode


### PickTreeNode


### DataAux

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|__computed |父节点|T | null|-|-|
|__computed |当前节点选中状态|CheckStatus|-|-|
|__computed |当前节点层级|number|-|-|
|__computed |节点位置顺序，可用来排序|string|-|-|
|__computed |保存节点位置路径信息|T[]|-|-|
|__computed |保存当前节点子节点分页信息|{     current: number;     pageSize: number;     total: number;   } | null|-|-|
|__computed |-|{ name: string; value: string }|-|-|
|__computed |-|{ name: string; value: T[] | null }|-|-|
|__computed |-|{ name: string; value: boolean }|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|__computed |当前节点的状态|() => {     disabled: boolean;     disableCheckbox: boolean;     checkable: boolean;   }|-|-|


### DataNodeType


### CheckStatus


### AuxOptions


### isCheckDisabled


### buildLevelMap

构建层级映射


### buildTree

将按顺序排列的树重新构建成树结构


### flatTree

拍平树结构，将树结构各个节点放置到`Map`中，并使用symbol记录父节点，方便还原树结构


### conductCheck

根据传入选中的节点集合与整棵树的对比，自动计算出当前应该的全选节点和半选状态


### wipeAuxiliary

清理辅助属性，保持数据干净性


### PickNormalProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|placeholder ~(可选)~ |-|string|-|-|
|keyPropName ~(可选)~ |唯一字段属性名 默认值为`key`|keyof T | null | ((value: T) => string)|-|-|
|titlePropName ~(可选)~ |标题属性名  默认为`title`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|descPropName ~(可选)~ |描述属性名 默认为 `desc`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|avatarPropName ~(可选)~ |头像属性名 默认 `avatar`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|showSearch ~(可选)~ |是否展示搜索框，默认`true`|boolean|-|-|
|scrollLoad ~(可选)~ |是否使用滚动加载，和`pagination`互斥，且优先级比`pagination`低， 如果未开启分页功能，则默认使用滚动懒加载，也可手动关闭懒加载|boolean|-|-|
|loadingConfig ~(可选)~ |-|Exclude<ListProps<T>["loading"], boolean>|-|-|
|pagination ~(可选)~ |-|ListProps<T>["pagination"] | boolean|-|-|
|maxHeight ~(可选)~ |列表最大高度，注意不是整个组件的最大高度|React.CSSProperties["maxHeight"]|-|-|
|minHeight ~(可选)~ |列表最小高度，注意不是整个组件的最小高度|React.CSSProperties["minHeight"]|-|-|
|height ~(可选)~ |列表高度，注意不是整个组件的高度|React.CSSProperties["height"]|-|-|
|emptyText ~(可选)~ |无数据展示的页面|React.ReactNode | (() => React.ReactNode)|-|-|
|showCheckAll ~(可选)~ |是否展示全选框  默认`true`|boolean|-|-|
|counter ~(可选)~ |计数区域定制|| boolean     | ((                  checkedItems: T[],                  list: T[]       ) => React.ReactNode)|-|-|
|rules ~(可选)~ |自定义规则 根据规则处理数据|{          checked?: (item: T, selectedItem?: T[]) => boolean;          disabled?: (item: T, selectedItem?: T[]) => boolean;          filter?: (item: T, selectedItem?: T[]) => boolean;   }|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|searchFilter ~(可选)~ |搜索过滤器，如果指定该函数则将对已有数据进行过滤筛选|(searchKey: string, items: T) => boolean|-|-|


### PickNormal


### PickTreeProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|showSearch ~(可选)~ |是否展示搜索框，默认`true`|boolean|-|-|
|showCheckAll ~(可选)~ |是否展示全选框|boolean|-|-|
|placeholder ~(可选)~ |-|string|-|-|
|childrenPropName ~(可选)~ |子级属性名|keyof T | null|-|-|
|isLeafPropName ~(可选)~ |叶子节点标志属性名 默认`isLeaf`|keyof T | null|-|-|
|keyPropName ~(可选)~ |唯一字段属性名 默认 `key`|keyof T | null | ((value: T) => string)|-|-|
|titlePropName ~(可选)~ |标题属性名  默认为`title`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|descPropName ~(可选)~ |描述属性名 默认为 `desc`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|avatarPropName ~(可选)~ |头像属性名 默认 `avatar`|keyof T | null | ((value: T) => React.ReactNode)|-|-|
|usingLazyLoad ~(可选)~ |是否使用懒加载，点击展开图标进行懒加载|boolean|-|-|
|preserveDataMode ~(可选)~ |保留数据的模式 默认`only-child`   - `only-child` 只保留子节点  - `priority-parent` 优先保留父节点(如果子节点全选则仅保留父节点)  - `all` 父节点和子节点都保留|"only-child" | "priority-parent" | "all"|-|-|
|preserveAuxiliaryProps ~(可选)~ |是否保留节点辅助属性，默认`false`,如果启用该属性，需要通过`symbol`才能获取属性|boolean|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|request ~(可选)~ |-|(     params?: {       searchKey?: string;     },     scene?: "init" | "lazy",     node?: T | null   ) => Promise<T[]>|-|-|
|filterNode ~(可选)~ |过滤函数|(searchKey: string, node: T) => boolean|-|-|


### PickTree


### DataNode


### PickTreeNode


### DataAux

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|__computed |父节点|T | null|-|-|
|__computed |当前节点选中状态|CheckStatus|-|-|
|__computed |当前节点层级|number|-|-|
|__computed |节点位置顺序，可用来排序|string|-|-|
|__computed |保存节点位置路径信息|T[]|-|-|
|__computed |保存当前节点子节点分页信息|{     current: number;     pageSize: number;     total: number;   } | null|-|-|
|__computed |-|{ name: string; value: string }|-|-|
|__computed |-|{ name: string; value: T[] | null }|-|-|
|__computed |-|{ name: string; value: boolean }|-|-|


### DataNodeType


### CheckStatus


### AuxOptions


### buildLevelMap

构建层级映射


### buildTree

将按顺序排列的树重新构建成树结构


### flatTree

拍平树结构，将树结构各个节点放置到`Map`中，并使用symbol记录父节点，方便还原树结构


### conductCheck

根据传入选中的节点集合与整棵树的对比，自动计算出当前应该的全选节点和半选状态


### wipeAuxiliary

清理辅助属性，保持数据干净性


### TabItemProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|key ~(可选)~ |对应 activeKey  注意：在当前`4.7.0`版本，使用 key 作为`activeKey`，为了兼容后续版本，这里对`key`和`tabKey`做了兼容，只需要填`tabKey`即可|string|-|-|
|tabKey |对应 activeKey|string|-|-|
|renderer ~(可选)~ |渲染器 支持两种方式  - 使用已定义的模板作为嵌套 - 使用自定义渲染器，参考 {@link PickRenderType}|Omit<IPick<D>, keyof PickPaneMustRequired<D>>|-|-|


### TabsChannelProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|tabPanes |面板配置|TabItemProps<D>[]|-|-|
|tabsConfig ~(可选)~ |-|TabsProps|-|-|


### TabsChannel


### CasBreadcrumbItem


### CascaderBreadcrumbProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|paths |-|(CasBreadcrumbItem & T)[]|-|-|
|breadcrumbPropName ~(可选)~ |-|string | ((path: T) => React.ReactNode)|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|onPathChange ~(可选)~ |路径切换回调|(path: T) => void|-|-|


### CascaderBreadcrumb


### TreeNodeItemProps

#### 属性

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|dataNode |-|T|-|-|
|selected ~(可选)~ |-|boolean | null | ((dataNode: T) => boolean)|-|-|
|halfSelected ~(可选)~ |半选状态|boolean | ((dataNode: T) => boolean)|-|-|

#### 方法

|名称|说明|类型|默认值|版本|
|:--:|:--:|:--:|:---:|:--:|
|onSelectedChange ~(可选)~ |-|(checked: boolean, node: T) => void|-|-|


### TreeNodeItem

